import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amag.captest3',
  appName: 'cap-angular-app',
  webDir: 'dist/cap-angular-app/browser'
};

export default config;
